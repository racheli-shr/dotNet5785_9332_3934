<Window x:Class="PL.MainWindow"
        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="MainWindow_Loaded"
        Closed="MainWindow_Closed">
    <Grid>

        <Grid>
            <Grid.RowDefinitions >
                <RowDefinition Height="36*" />
                <RowDefinition Height="15*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Content="{Binding CurrentTime}" FontWeight="Bold" FontSize="15" Grid.Row="0" Grid.Column="1" Margin="24,54,34,242" Height="40"/>
            <Button IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Add One Minute" Click="BtnAddOneMinute_Click" Grid.Row="0" Height="40" Grid.Column="1" Margin="15,77,15,184" />
            <Button IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Add One Day" Click="BtnAddOneDay_Click" Grid.Row="0" Height="40" Grid.Column="1" Margin="15,121,15,140" />
            <Button IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Add One Hour" Click="BtnAddOneHour_Click" Grid.Row="0" Height="40" Grid.Column="1" Margin="15,165,15,96" />
            <Button IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Add One Month" Click="BtnAddOneMonth_Click" Grid.Row="0" Height="40" Grid.Column="1" Margin="15,209,15,52"  />
            <Button IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Add One Year" Click="BtnAddOneYear_Click" Grid.Row="0" Height="40" Grid.Column="1" Margin="15,252,15,9"  />
            <!--go to volunteer list-->
            <Button Content="Hundle Volunteer" Height="50" Name="BtnHandleVolunteer" Click="HandleVolunteer_Click" Grid.Row="1" Grid.Column="1" Margin="28,12,36,63"  />
            <!--go to Call list-->
            <Button Content="Hundle Call" Height="50" Name="BtnHandleCall" Click="HandleCall_click" Grid.Row="1" Grid.Column="1" Margin="28,67,36,8"  />
            <Button Height="50" IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Reset DB" Grid.Row="1"  Margin="32,68,32,7" RenderTransformOrigin="19.875,145.65" Click="BtnResetDB"/>
            <Button Height="50" IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Content="Initialize DB" Grid.Row="1" Margin="32,13,32,62" Click="BtnInitializeDB" RenderTransformOrigin="0.5,0.5">
                <Button.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Button.RenderTransform>
            </Button>
            <TextBox IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Text="{Binding RiskRange, Mode=TwoWay}" Margin="68,128,68,154" TextChanged="TextBox_TextChanged" />
            <Label IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Margin="88,8,88,208" Content="RiskRange"/>
            <Button Click="BtnUpdateRiskRange_Click" Content="update the Risk Range" Margin="45,44,45,221"/>
            <TextBlock Height="66" Text="{Binding volunteer.FullName}" Grid.Column="1" HorizontalAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Top"  Width="200" Margin="0,10,0,0"/>
            <Button Content="{Binding SimulatorButtonText}" Click="startOrStopSimulator" Margin="45,52,44,229" Grid.Column="2"/>
            <TextBox  IsEnabled="{Binding EnableToChange, Mode=TwoWay}" Text="{Binding Interval}" Margin="62,122,62,166" Grid.Column="2"/>
            <ScrollViewer Grid.Column="2" Grid.Row="0"  Margin="10,203,10,15" VerticalScrollBarVisibility="Auto" Grid.RowSpan="2">
                <DataGrid
        ItemsSource="{Binding CallStatusSummaries}"
        SelectedIndex="{Binding SelectedStatus}"
        AutoGenerateColumns="False"
        CanUserReorderColumns="False"
        CanUserResizeRows="False"
        CanUserSortColumns="False"
        IsReadOnly="True"
        SelectionMode="Single"
        SelectionUnit="FullRow"
        
        Margin="10" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="status" Binding="{Binding Status}" />
                        <DataGridTextColumn Header="amount" Binding="{Binding Amount}" />
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </Grid>

    </Grid>

</Window>
